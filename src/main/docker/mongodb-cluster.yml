version: '2'
services:
  spotgarage-mongodb:
    image: mongo:4.0.13
    ports:
      - '27017:27017'
    command: mongos --configdb spotgarage-mongodb-config
  spotgarage-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --replSet rs1 --noprealloc --smallfiles
  spotgarage-mongodb-config:
    image: mongo:4.0.13
    container_name: spotgarage-mongodb-config
    command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
